[project]
name = "nanobot"
version = "0.1.0"
description = "Nanobot agent for LMS"
requires-python = ">=3.14"
dependencies = [
    "nanobot>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
